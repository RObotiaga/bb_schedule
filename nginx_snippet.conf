# --- ВСТАВИТЬ В КОНФИГУРАЦИЮ NGINX (внутри server { ... }) ---
# Обычно это находится в настройках 3x-ui панели или в /etc/nginx/sites-enabled/default

location / {
    proxy_pass http://127.0.0.1:8010;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# Опционально: статические файлы (если понадобится, но FastAPI их может раздавать сам через StaticFiles, если настроить)
# location /static/ {
#     proxy_pass http://127.0.0.1:8000/static/;
# }
